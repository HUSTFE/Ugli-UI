/**
 * Combine two object and return the combined object.
 * @param first
 * @param second
 */
export default function combine(first?: {
    [k: string]: any;
} | null, second?: {
    [k: string]: any;
} | null): object;
